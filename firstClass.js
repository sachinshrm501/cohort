//NodeJs is like a company manager 
//Sales person like as express (handle the user request)
//WorkShop as a database 
// Initially JS run only in browsers
//NodeJs and burn both are the same but the difference is that nodejs is a runtime environment and burn is a browser runtime environment
//NodeJs is a runtime environment that allows us to run JS code on the server
//() // parenthesis
function printChain(){
    console.log('Hello World');
}
printChain();
//`` // backtick template string
function printAmout(amount){
    console.log(`Amount: ${amount}`);
}
printAmout(100);